# Roadmap

Last reviewed: 2026-06-30

This file is the canonical public roadmap and implementation-status tracker for
Flux-LLM-KB. It is intentionally separate from live runtime state. Do not add
private paths, mail contents, OAuth tokens, live database values, raw indexed
content, local deployment-only details, or private memory exports here.

Every roadmap-significant session or turn must update affected `Progress %` and
`Remaining Work` entries before closeout. Percentages are conservative planning
estimates toward `shipped`; they are not live runtime health measurements.

Live operational state belongs in the local dashboard and production status
scripts:

- Dashboard: `http://127.0.0.1:8765/dashboard`
- Production status: `scripts/deploy/status-flux.ps1`
- CLI health: `flux-kb doctor --json`

## Status Model

| Label | Meaning |
| --- | --- |
| `shipped` | Implemented, documented enough to use, and covered by verification. |
| `in progress` | Usable slice exists, but planned scope remains. |
| `planned` | Roadmap intent exists; implementation has not started. |
| `blocked` | Cannot proceed without external input, dependency, or design decision. |
| `deferred` | Intentionally postponed. |

## Priority Model

The detailed roadmap tables and queued work use the same priority labels:

| Label | Meaning |
| --- | --- |
| `P0` | Core-value or correctness work that should come before broader automation. |
| `P1` | Basic feature completion and reliability hardening that should follow the P0 foundation. |
| `P2` | Automation, enrichment, and optimization work that should wait for evaluation signals. |
| `P3` | Deferred exploration that should not distract from single-user reliability. |

## Version Summary

| Version | Plain-English Purpose | Status | Progress % | Summary | Remaining Work |
| --- | --- | --- | --- | --- | --- |
| V0 Foundation | Put the project on safe, repeatable footing before adding memory features. | shipped | 100% | Public repo, safety model, ADRs, PostgreSQL/pgvector migrations, Docker Compose, fixtures, and initial interfaces exist. | Maintenance only: keep public/private boundaries, migrations, fixtures, and interfaces aligned as later features land. |
| V1 Working Knowledge Kernel | Store useful agent knowledge in a searchable system instead of relying on one giant memory file. | in progress | 87% | Core storage, CLI/REST/MCP surfaces, redaction/audit, wiki export, hybrid retrieval, balanced generic retrieval guardrails, Codex hooks, Codex MCP setup, production MCP container execution, safe reference capture, and V1 graph/lifecycle backend hardening exist; review UI depth remains future work. | Finish deeper review UX proof, live-session capture tuning, retrieval explainability feedback loops, and real-session validation of the containerised production MCP path. |
| V2 Review And Visualization | Give the operator a dashboard to inspect, correct, and trust what Flux remembers. | in progress | 94% | React dashboard is the unified operational UI; graph browsing, claim lifecycle review, capture-review visibility, approval/rejection decisions, approved-backfill ingestion, audit-visible rationales, retention policy tuning, memory quality reporting, friendly Overview, guarded Automation, focused Diagnostics, split API/host database health, Performance evidence, retrieval focus controls, code feedback, filtered operational diagnostics, row-level remediation actions, source-aware mail profile setup, and a comprehensive real-GUI dashboard manual with public-safe generated screenshots exist. | Validate the IA and split database health against live operator use, update the manual only on explicit request, complete deeper exception-oriented review workflows, and add higher-value recovery pages only where repeated diagnostics justify them. |
| V2.5 Autonomous Corpus Expansion | Let Flux safely notice and index useful local files without constant manual imports. | in progress | 94% | Watch roots, host agent, reconciliation, worker processing, duplicate/version suppression, strict-indexing root policy, broad file-type roadmap, structured diagram extraction, business document extractor expansion, converted legacy/OpenDocument spreadsheet sample-first profiling, bounded recursive archive/container extraction, strict-root container parent completion when extracted child assets carry the content, safe partial archive parent completion, strict-root completed-no-content handling for successful empty image/Office/PDF extraction, practical processor-pack installation for Docker/host Python modules, practical local parsers for subtitles, mail exports including `.msg` conversion through local `msgconvert`, calendar/contact files, structured/report data, SARIF/SPDX/CycloneDX, JUnit/TRX/TAP, LCOV/coverage XML, HAR, SQLite schema metadata, and sensitive metadata-only formats, plus expanded practical code parsers for C#, Python class relationships, frontend markup, and stylesheets exist; deeper media/vector and proprietary local-tool parsing remain planned. | Deploy, requeue, and backfill remaining real strict-indexing blockers with extractor evidence or explicit exclusions, then finish specialized file-type stages, richer media/vector diagnostics, optional full structured backfill, proprietary CAD/BI/geospatial/scientific local-tool proofs, live sync-client proof, and all-root strict-indexing evidence. |
| V2.6 Mail Capture And Runtime Configuration | Make mail capture and runtime settings manageable from the local app instead of hidden scripts. | in progress | 99% | Settings catalog, production deployment, Gmail OAuth, IMAP capture, PostgreSQL-safe claimable IMAP scheduler state, stale active-run recovery, worker-startup orphan recovery, Outlook host split, stale Outlook host reporting, pending Outlook request cancellation, stale Outlook claim recovery, host-side `/app/private` spool mapping, path-scoped background `corpus_sync_root` jobs for changed Outlook spool exports instead of blocking host HTTP calls or rescanning the full spool, rich Outlook body/HTML/attachment export after programmatic-access validation, deterministic Outlook `.eml` MIME boundaries with safe spool duplicate reporting/reclaim, Outlook selected-folder child traversal with an explicit include-subfolders profile option, Outlook COM incremental cursors with received-time and last-modification-time modes, ascending COM timestamp traversal with safe mid-run cursor checkpoints, no-cursor EntryID duplicate skipping, single-host Outlook locking with process-ID heartbeats, loop-level Outlook host heartbeats while idle or claim-blocked, repeating self-healing scheduled host tasks, strict-by-default mail roots, runtime-inaccessible mail spool blocking before corpus reconciliation, source-aware dashboard controls, provider-specific non-destructive post-processing, and consumer access exist; broader live-provider validation should continue. | Broaden live-provider validation, keep rollout profiles on non-destructive post-processing first, validate deployed self-healing single-host Outlook catch-up completion across nested folders and large folders with path-scoped spool indexing and post-deploy safe duplicate reclaim, and add isolated/time-bounded rich COM export only if programmatic-access prompts recur. |
| V2.8 Indexer Acceleration And Local Inference Optimization | Make large local indexing faster, observable, and easier to tune on one PC. | in progress | 99% | Dedicated acceleration lane foundations exist for local capability status, explicit watcher backend policy/probe, cache layout visibility, worker-family queues and caps, backpressure/debug status, bounded crawler hash parallelism, incremental scan manifest skips, throughput telemetry, cache-backed local OCR/ASR, recursive container telemetry, parser and vision cache telemetry, Docker Compose Ollama local model service access for vision with a persistent production model cache, qwen3-vl:8b as the production vision default with a 2-minute keepalive policy, loopback-only Docker Ollama publishing plus persisted host-side runtime settings for Windows host-agent image work, video frame sampling, thumbnail cache reuse, embedding vector refresh jobs, stale non-indexed chunk/vector repair, durable benchmark history, strict-root metadata-only blockers in reliability cards, scenario diagnostics, all-root evidence runs, operator evidence gates, and a focused dashboard Performance tab. Watcher/service heartbeats now stay independent of reconciliation and event indexing, host-agent watcher/worker loops keep retrying through transient PostgreSQL restart/write failures and clear recovered worker errors, watch events enqueue coalesced `corpus_sync_root` work instead of indexing inline, watchers honor effective glob policies before emitting events, dedicated `.worktrees/**` folders are excluded by default, watch-triggered path syncs and Outlook export deltas batch scoped paths instead of widening immediately to full-root jobs, stale running root-sync jobs recover globally from progress heartbeats while worker heartbeats no longer mask stuck progress, excluded trees are pruned before crawler/watcher descent, watchdog stability polling avoids full snapshots when no pending stable candidates are pending, `workers=N` runs claimed corpus jobs concurrently, local defaults are tuned for a 12-core/24-thread workstation while staying overrideable, local PostgreSQL compose settings now use a high-memory Docker-visible profile for retrieval/indexing throughput, production Docker I/O uses named volumes for container-owned PostgreSQL/cache/runtime/log/model state with a guarded legacy bind-data migration path, and production GPU dependency installation has bounded Docker/pip timeouts plus an explicit package-index override for slow large wheel fetches. | Run and compare live high-volume all-root evidence after deployment, validate Docker memory ceiling, Docker-volume migration/reuse, qwen3-vl:8b model pull, loopback host-agent image backfills, 2-minute local vision keepalive, and broader local vision blocker backfills, validate the high-memory PostgreSQL profile against observed temp-file, checkpoint, disk, DB, and Docker memory pressure, validate stale root-sync recovery and path batching under live watcher/Outlook churn, validate stale chunk/vector repair on production mail roots, and keep VSS/provider acceleration future-only until the gate justifies design work. |
| V2.7 Mail And Retrieval Production Hardening | Make search, mail handling, and file indexing reliable enough for daily use. | in progress | 99% | Search result actions, in-app mail/file detail views, host-agent file actions, logical mail grouping, managed-mail body/attachment indexing through private disk sidecars instead of PostgreSQL plaintext chunk bodies, raw mail backup skipping, structured diagnostics over current benchmark schemas, claimable IMAP sync runs with stale running/claimed recovery and worker-startup orphan recovery, Outlook host loop exception hardening plus active long-run heartbeats, loop-level heartbeats during idle/claim-blocked host work, self-healing scheduled host relaunch with duplicate-instance prevention, process-ID Outlook host heartbeats, single-host Outlook lock enforcement, safe Outlook cursor checkpoints during long runs, immediate stale-heartbeat Outlook claim recovery, rich Outlook export after live programmatic-access checks, Outlook child-folder traversal bounded by max messages per run, path-batched Outlook spool indexing jobs with stage/path/file progress telemetry, extractor exception containment, OCR timeout terminal blocking, missing-source attachment cancellation/deleted terminal handling, invalid-package attachment blocking, PDF missing-crypto dependency blocking, provider-specific mail post-processing, lock-tolerant indexing/watch states, strict metadata-only retrieval suppression, indexed-only corpus retrieval/vector/semantic candidates, root-aware embedding metadata, HNSW-first root-filtered vector candidates, trigram-indexable fuzzy/code-symbol candidates, balanced generic ranking guardrails, code-explicit broad retrieval exclusion, Codex hook non-code preflight fallback, dashboard search focus filters, single-pass corpus hydration, sidecar-hydrated mail search/detail/embedding backfill, deleted-row-aware manifest missing-chunk repair, blocked mail spool sync when the current runtime cannot see the configured private root, split service/host PostgreSQL health diagnostics, containerised production Codex MCP execution, Mail Errors panel separation from corpus extraction failures, query-aware snippets, retrieval/brief explainability with stream timings, configurable retrieval filters, suppression/lifecycle diagnostics, filtered operational drill-downs, confirmation-gated diagnostic remediation actions, and guarded automation audit rationale for safe passes exist. | Validate code-explicit broad retrieval, split database health, containerised Codex MCP, and guarded-action wording against live failures; deploy and backfill remaining strict-indexing blockers on real roots with extractor evidence or explicit exclusions, compare tuned PostgreSQL settings on the live compose runtime, continue live Outlook COM validation for nested-folder, large-folder resumability, path-scoped spool indexing, and host self-healing, then finish cloud-sync proof and semantic media follow-through. |
| V3 Scale And Evaluation | Prove Flux improves retrieval and memory quality before trusting more automation. | in progress | 72% | Code-aware corpus indexing, historical backfill, retrieval benchmarks, automated memory governance, optional ParadeDB/BM25, and local-only librarian workers. Code-aware foundations, code-explicit broad retrieval gating, balanced generic/code-focused ranking separation, expanded parser/relationship coverage, metadata-only retrieval benchmark calibration, governance-shadow evaluation, approved Codex backfill ingestion, privacy-safe code retrieval feedback, evaluated governance proposals, guarded apply/recover, operator digests, default-off librarian scheduling, default-off guarded operator automation, and live exact-code-symbol ranking tuning now exist. | Use live repository, explicit-code, and non-code prompt feedback plus benchmark failures to drive parser/ranking work, live governance calibration, safe automation cadence tuning, deeper local-model-assisted consolidation, optional backend decisions, and broader historical backfill discovery. |
| V4 Collaboration And Transfer | Explore sharing and transfer only after the personal local system is safe and stable. | planned | 0% | Shared vault mode, sync/export policy, optional Apache AGE, and synthetic-data/fine-tuning pipeline. | Start only after single-user retrieval, diagnostics, governance, recovery, and privacy controls are stable. |

## V0: Foundation

| Piece | Priority | Plain-English Purpose | Roadmap Intent | Status | Progress % | Current Evidence / Remaining Gap | Remaining Work | Queued Next |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Public project baseline | P0 | Keep the public repo safe to share while the private runtime stays private. | Public GitHub repo with safety docs, architecture records, and public/private data boundaries. | shipped | 100% | Safety docs, architecture docs, and repository guidance exist. | Maintain boundaries during every roadmap-significant update. | Maintain boundaries during every roadmap-significant update. |
| Durable store | P0 | Give Flux a dependable database so memories survive restarts and upgrades. | PostgreSQL plus pgvector schema migrations as the primary persistence backend. | shipped | 100% | PostgreSQL/pgvector migrations are present and remain the preferred backend. | Keep migrations backward-compatible as later features expand tables. | Keep migrations backward-compatible as later features expand tables. |
| Runtime bootstrap | P1 | Make startup failures obvious instead of leaving operators guessing. | Docker Compose runtime profile with explicit Docker prerequisite checks. | shipped | 100% | Docker setup and doctor/status scripts fail clearly when prerequisites are missing. | Keep deployment scripts aligned with production layout changes. | Keep deployment scripts aligned with production layout changes. |
| Test fixtures | P1 | Let tests cover indexing behavior without using private files. | Synthetic fixture corpus for repeatable tests. | shipped | 100% | Fixtures support repeatable local tests without private content. | Expand fixtures only with synthetic data. | Expand fixtures only with synthetic data. |
| Initial interfaces | P1 | Give people and tools stable ways to talk to Flux from the start. | Initial MCP, CLI, and REST skeletons. | shipped | 100% | CLI, REST, and MCP-facing service layer exist. | Preserve surface compatibility while adding features. | Preserve surface compatibility while adding features. |

## V1: Working Knowledge Kernel

| Piece | Priority | Plain-English Purpose | Roadmap Intent | Status | Progress % | Current Evidence / Remaining Gap | Remaining Work | Queued Next |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Storage and retrieval core | P0 | Help Flux find relevant facts by combining exact words, meaning, relationships, and freshness. | Hybrid retrieval with lexical, vector, graph, and lifecycle scoring. | in progress | 83% | PostgreSQL full-text, pgvector, local deterministic `flux-hash-v1` embeddings, JSONB/GIN, trigram fuzzy matching, root-aware corpus embedding metadata, HNSW-first vector candidate selection, candidate-first hydration, hybrid RRF ranking, balanced non-code guardrails for broad corpus queries, durable claim lifecycle events, claim relations, bounded typed graph traversal, lifecycle scoring decay, and CLI/REST/MCP graph/claim primitives exist. V2 review UX and deeper retrieval explainability remain planned. | Add V2 graph browsing, stale claim review, contradiction review, capture approval, retention tuning, and live ranking calibration after this backend contract. | Add V2 graph browsing, stale claim review, contradiction review, capture approval, retention tuning, and live ranking calibration after this backend contract. |
| Codex integration | P0 | Bring relevant Flux context into Codex work automatically and save useful outcomes afterward. | Codex hooks for automatic preflight retrieval and session capture. | shipped | 100% | Automatic non-trivial prompt briefs, non-code fallback reruns for code-heavy preflight matches, final-turn capture, opt-out settings, dashboard-visible status, and audit records exist. | Continue real-session proof across Codex surfaces, long-running turns, opt-out habits, and duplicate capture review. | Continue real-session proof across Codex surfaces, long-running turns, opt-out habits, and duplicate capture review. |
| Hook policy | P1 | Let users control when Codex memory is used so small or sensitive prompts are not over-handled. | Configurable Codex hook policy with relevance gating, context-budget limits, opt-out controls, audit records, dashboard health/status, and suppression for trivial prompts. | shipped | 100% | Hook policy controls and status are available through settings, dashboard, and audit flow. | Keep policy tuning tied to real-session feedback. | Keep policy tuning tied to real-session feedback. |
| Codex MCP/plugin setup | P1 | Make Flux tools available inside Codex without hand-editing every integration file. | Codex personal plugin and MCP configuration for callable Flux tools. | shipped | 100% | `flux-kb codex install-plugin` configures MCP; production app roots run MCP through the API container, while development roots keep the local Python launcher. Codex may expose raw names such as `kb.brief` or wrappers such as `mcp__flux_llm_kb.kb_brief`. | Keep installer diagnostics current when Codex plugin/MCP discovery changes, and validate production container MCP after deployment. | Keep installer diagnostics current when Codex plugin/MCP discovery changes. |
| Redaction and audit | P0 | Reduce privacy risk and leave a record of what changed. | Redaction and audit trail before any persistence. | shipped | 100% | Public docs and implementation require redaction and audit records before persistence; manual memory and claim write paths redact user-supplied text before database writes. | Extend audit coverage as new write paths are added. | Extend audit coverage as new write paths are added. |
| Human audit export | P2 | Let a person review remembered knowledge outside the app. | Markdown wiki export for human auditability. | shipped | 100% | Wiki export exists as a CLI/runtime capability. | Keep exports free of private generated artifacts in the public repo. | Keep exports free of private generated artifacts in the public repo. |
| Integration tests | P1 | Catch database and contract breakage before changes reach the running system. | Disposable PostgreSQL integration tests. | shipped | 100% | Disposable database tests exist for core flows. | Add targeted integration coverage when graph/lifecycle work lands. | Add targeted integration coverage when graph/lifecycle work lands. |

## V2: Review And Visualization

| Piece | Priority | Plain-English Purpose | Roadmap Intent | Status | Progress % | Current Evidence / Remaining Gap | Remaining Work | Queued Next |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Unified operations dashboard | P1 | Put health, review, search, and controls in one place so operators do not need raw logs. | Unified dashboard for health, monitoring, watcher status, crawler stats, search, graph browsing, stale claims, contradictions, capture review, runtime settings, and mail ingestion status. | in progress | 96% | React/Vite dashboard served by FastAPI is the unified operations UI for friendly read-only Overview, guarded Automation, Diagnostics, Performance, corpus monitoring, runtime settings, source-aware mail capture setup, worker state, Outlook COM host status, Outlook sync request visibility/cancellation from Jobs, corpus sync job progress/cancellation with explicit running-job conflict feedback, Mail Errors separation from corpus extraction failures, claim lifecycle review, selected-entity graph browsing, retention tuning, memory quality reporting, Retrieval search focus controls, code-heavy result diagnostics, capture-review queue decisions, approved-ingestion status/counters, governance-shadow benchmark evidence, and recent decision/ingestion audit visibility. | Continue validating tab ownership, action wording, retrieval focus defaults, and exception-oriented review workflows with live operator use; keep retention review exception-oriented for rare escalations, policy tuning, audits, and recovery once automatic retention workers exist. | Update the dashboard manual only when the operator explicitly asks for manual changes; otherwise use live dashboard and tests as the baseline for UI/API changes. |
| Retention and quality | P2 | Help decide which memories stay useful, fade out, or need review. | Retention policy tuning and memory quality reports. | shipped | 100% | Fixed retention classes for claim, episode, and corpus are tunable through REST, CLI, MCP, and the dashboard Review tab; updates are audited, and quality reports aggregate sanitized claim, episode, and corpus candidates without raw content or automatic mutation. | Calibrate policy defaults from live review feedback, then use them as guardrails for future automatic low-risk lifecycle tagging, deprioritization, and duplicate suppression while keeping public docs free of private runtime values. | Calibrate policy defaults from live review feedback, then use them as guardrails for future automatic low-risk lifecycle tagging, deprioritization, and duplicate suppression while keeping public docs free of private runtime values. |
| Sensitive capture review | P1 | Put questionable captures in front of a person before they become trusted memory. | Manual approval flows for sensitive or low-confidence captures. | in progress | 90% | Capture and audit paths exist, and the dashboard now exposes status-filtered capture review with approval/rejection actions, required rationale capture, approved Codex backfill ingestion, sanitized ingestion rows, created memory ids, blocked/failed/skipped status, and recent `capture.review_*` plus `capture.ingestion_*` audit visibility. | Continue live usage feedback, tune exception handling from real approved backfill runs, and keep automatic apply/recover and periodic digests for the later governance automation bundle. | Continue live usage feedback, tune exception handling from real approved backfill runs, and keep automatic apply/recover and periodic digests for the later governance automation bundle. |
| Drill-down diagnostics | P1 | Let operators investigate why Flux behaved a certain way instead of guessing. | Dashboard drill-down views for retrieval explanations, watcher events, and worker history. | in progress | 89% | Dashboard now has a focused Diagnostics tab for operational panels plus row-level diagnostics for retrieval explain traces, watcher events, worker heartbeat/history, slow jobs, blocked dependencies, mail sync runs, stale IMAP scheduler runs, and mail post-process state. Rows include filters, bounded evidence, follow-up commands, sanitized targets, copy/detail/navigation actions, and confirmation-gated remediation actions for retry, scoped backfill, and root cleanup where supported. | Validate recovery actions against recurring live failures, then add only higher-value detail/recovery pages that operators actually use. | Use live diagnostic patterns to decide whether deeper row pages, retention recovery, or additional scoped remediation actions are warranted. |

## V2.5: Autonomous Corpus Expansion

| Piece | Priority | Plain-English Purpose | Roadmap Intent | Status | Progress % | Current Evidence / Remaining Gap | Remaining Work | Queued Next |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Watch roots and host bridge | P0 | Let Flux safely watch folders on the real machine even when the app runs in Docker. | Configurable recursive path monitoring plus local host-agent bridge for arbitrary host filesystem paths when API/dashboard run in Docker. | shipped | 100% | Host agent supports Windows/host paths, folder browse, validation, watch, sync, reconciliation, and host-side worker processing. | Continue compatibility validation across host platforms and sync clients. | Continue compatibility validation across host platforms and sync clients. |
| Include/exclude policy | P1 | Let users decide which files matter and which should be ignored. | Global include/exclude glob defaults with per-root inherit, extend, or override behavior visible in the dashboard. | shipped | 100% | Settings catalog and dashboard expose effective root policy. | Keep policy explanations clear as file-type support expands. | Keep policy explanations clear as file-type support expands. |
| Live watcher runtime | P0 | Notice file changes without taking ownership of files or interrupting other apps. | Reloadable enabled roots, debounce, bounded queues, heartbeat, stale-state reporting, and non-invasive watch semantics. | shipped | 100% | Watcher reload, heartbeat, stale-state reporting, stable-candidate gating, and non-exclusive semantics exist. | Broaden real-world validation against OneDrive, SharePoint, Dropbox, editors, build tools, and backup software. | Broaden real-world validation against OneDrive, SharePoint, Dropbox, editors, build tools, and backup software. |
| Reconciliation and targeted sync | P0 | Recover from missed file events and update only the changed parts when possible. | Startup and periodic reconciliation plus targeted file/subtree sync for watcher-triggered updates. | shipped | 100% | Startup/periodic reconciliation and targeted sync are implemented for enabled watched roots. | Use the V2.8 benchmark harness when validating high-volume roots and watcher-triggered updates. | Use the V2.8 benchmark harness when validating high-volume roots and watcher-triggered updates. |
| File-type coverage matrix | P1 | Make clear what Flux can read now, what it only tracks as metadata, and what still needs work. | Broad file-type coverage through explicit support tiers and a dedicated coverage matrix. | in progress | 90% | Public `file-type-coverage.md` and broad roadmap targets exist; strict-indexing roots now block metadata-only outcomes instead of treating them as searchable knowledge. Structured Draw.io, modern VSDX extraction, local business document expansion for Office variants, OpenDocument files, legacy Excel/PowerPoint adapters, converted large legacy/OpenDocument spreadsheet sample-first profiles, EPUB/FB2 extraction, Calibre `ebook-convert` eBook fallback, comic archive container handling, bounded recursive archive/container extraction, embedded media sidecar transcripts, practical Docker/host processor-pack installation, practical local parsers for subtitles, mail exports, calendar/contact files, PSV/SSV/NDJSON/JSON-LD, SARIF/SPDX/CycloneDX, JUnit/TRX/TAP, LCOV/coverage XML, HAR, SQLite schema metadata, sensitive metadata-only formats, expanded C#/Python/frontend/style code parsers, cache-backed local OCR for image/image-only PDF jobs, and cache-backed local ASR for audio/video jobs exist; many advanced proprietary formats remain metadata-first or optional-tool dependent. | Validate processor availability after rebuild/deploy and continue extractor expansion only where safe local parsers or optional local tools have public-safe tests and operator diagnostic value; for go-live roots, install the extractor or exclude unsupported families before declaring ready. | Prioritize specialized local-tool stages only after live diagnostics identify repeated gaps. |
| Text/code/data extraction | P1 | Read common plain-text, code, and data files quickly without expensive tools. | Inline text extraction and local parser extraction for text, code, developer artifacts, structured data, and common exports. | in progress | 91% | Small text-like files, BOM-marked UTF-8/UTF-16/UTF-32 text/code decoding, PostgreSQL-safe NUL stripping at text/JSON write boundaries, expanded code parser paths for Python class decorators/inheritance, C# namespaces/controllers/routes/tests/calls, frontend markup components/events/actions/selectors, stylesheet selectors/custom properties/keyframes/media queries, sample-first large CSV/TSV/PSV/SSV/JSON/JSONL/NDJSON/JSON-LD/workbook indexing, converted legacy/OpenDocument spreadsheet sample-first profiling, and bounded local report/export summaries are supported. Optional full structured backfill and deeper framework-specific parsers remain planned. | Add optional full structured backfill only after sample-first metadata proves useful, and broaden local parser coverage where public-safe tests or live code feedback can cover it. | Extend structured-file backfill and parser coverage only where local parsing is reliable and diagnostic evidence shows value. |
| Documents, spreadsheets, and presentations | P1 | Extract useful text from normal business files people already have. | Local extraction for PDF, Office, OpenDocument, ebooks, scans, speaker notes, tables, and embedded media metadata. | in progress | 77% | Common PDF/Office extraction exists; Office macro/template variants, OpenDocument text/spreadsheet/presentation files, legacy Excel/PowerPoint local adapters, converted oversized spreadsheet sample-first profiling, bounded OCR fallback for image-only PDFs, EPUB/FB2 local publication parsing, Calibre `ebook-convert` fallback for MOBI/AZW/LIT, comic archive container extraction, and embedded document parsing from bounded containers now have extraction paths. Broader document families still need staged local-tool handling. | Prioritize remaining local-tool support with metadata-first fallback. | Prioritize remaining local-tool support with metadata-first fallback. |
| Mail and collaboration exports | P2 | Bring exported conversations and attachments into search without treating them as loose files. | Treat EML, MSG, mbox, maildir, calendar/contact, chat exports, transcripts, and attachments as first-class indexed relationships. | in progress | 79% | IMAP/Outlook mail enters a private spool, new mail roots are strict by default, `manifest.json` is indexed as normal metadata, managed `body.txt` and `attachments/` chunk text is stored in private disk sidecars with PostgreSQL chunk bodies blanked, Outlook COM exports rich body/HTML, `.msg` backup, and attachment files when programmatic access is allowed, raw `.eml`/`.msg` backups and duplicate `body.html` artifacts are skipped from duplicate text indexing, empty mail body sidecars complete as no-content results, attached `.msg` files convert through local `msgconvert`, OCR-timeout and invalid-package attachment jobs block terminally, missing-source attachment jobs cancel as deleted instead of inflating blockers, inaccessible spool roots block sync before delete reconciliation, deleted-row-aware manifest repair repopulates missing chunks for unchanged indexed assets, and related-evidence grouping exists for mail results. PST/OST and broader collaboration exports remain optional/future. | Continue live attachment validation, keep mail spool logical grouping, requeue/backfill remaining strict blockers after deployment, and add PST/OST/collaboration exporters only after optional-tool or isolated-process evidence justifies them. | Use live mail attachment results and diagnostics to prioritize the next export family. |
| Images, diagrams, and vector assets | P2 | Make visual files searchable through local structure, OCR, or optional local captions. | Structural extraction, image hash cache reuse, local OCR, optional local vision descriptions, and metadata-first handling for design/vector assets. | in progress | 77% | Draw.io, embedded Draw.io SVG/PNG payloads, and modern VSDX/VSDM/VSSX/VSSM/VSTX/VSTM containers are parsed locally into diagram chunks. Deferred image jobs can run local Tesseract OCR with redacted cache reuse and dashboard-visible cache hit/miss telemetry; image-only PDF OCR keeps 200 DPI while capping rendered image edges at 6000 pixels, native images over 6000 pixels are downscaled before Tesseract, and OCR cache keys remain tied to original source files with preprocessing metadata. Tesseract and `pdftoppm` timeouts now become terminal blocked extraction states instead of endless pending retries. Decorative-image spacers and small inline icons are skipped before OCR or vision and count as completed no-content extraction on strict roots. Empty OCR results escalate to configured local vision before strict roots complete as no-content. Combined OCR+vision and transcript+frame-vision chunk streams are reindexed sequentially before persistence, preventing duplicate asset chunk indexes when multiple visual enrichers produce text. Optional local vision descriptions run through configured local loopback, Docker host-gateway, internal Docker Ollama inference, or the loopback-published Docker Ollama port used by Windows host-agent roots when `acceleration.vision.enabled` and `acceleration.vision.model` are configured. Production GPU mode enables bounded Qwen local vision through Docker Ollama when available, with 1280-pixel submitted vision copies, a larger generation budget, and Qwen thinking-field fallback when Ollama returns no normal response text. Redacted vision cache telemetry is exposed, and failed model attempts carry status/error telemetry even when OCR still completes the image job. Deeper vector/design extractors remain planned. | Validate the MoHESR and other requeued live image/archive blockers after Docker Ollama GPU deployment, then calibrate local vision quality from retrieval benchmarks and add deeper vector/design extractors only where evidence supports it. | Requeue/backfill live image blockers through local vision, then use retrieval evidence to prioritize deeper vector/design extraction. |
| Audio, video, and subtitles | P2 | Let Flux use transcripts, local speech tools, and frame previews without sending media to the cloud. | Sidecar transcript indexing, optional local ASR, media metadata, stale lock recovery, and semantic media backfill. | in progress | 67% | Media sidecars and metadata are first-class targets, including embedded media sidecar transcripts from archives; local faster-whisper ASR can transcribe bounded audio/video jobs through `ffmpeg` when `acceleration.asr.model_path` points at an existing local model. Redacted ASR cache entries record cache hits, misses, and segment counts without cloud transcription or remote model download. Video jobs can use scene-transition frame sampling with thumbnail cache reuse and midpoint fallback only when no transition is detected. Richer media diagnostics and semantic media backfill remain deferred. | Add stale lock recovery proof, richer segment/frame diagnostics, and semantic media backfill. | Add stale lock recovery proof, richer segment/frame diagnostics, and semantic media backfill. |
| Archives and containers | P1 | Safely look inside zip-like packages without letting huge or unsafe archives overwhelm the system. | Bounded archive/container expansion with depth, size, and file-count caps. | in progress | 82% | ZIP-family, TAR-family, gzip/bzip2/xz streams, supported package containers, and comic archive formats are enumerated through bounded local adapters; optional-tool formats report explicit dependency states when local tools are missing; inline-safe text/code members, nested containers, embedded documents, diagrams, images, and media become related child assets with sanitized recursion telemetry. Strict roots now complete container parent rows when extracted child assets carry the content, including safe partial parents with oversized skipped members, while dependency-blocked containers remain actionable blockers. Embedded media sidecar transcript files are used before probing or ASR while remaining visible as child assets. | Validate partial-container outcomes against live blocked-member evidence, then add broader optional-tool validation and deeper specialized local-tool stages after worker-family scheduling and observability mature. | Requeue/backfill live archive blockers and tune archive caps only from observed partial/container diagnostics. |
| Worker claiming and retry | P0 | Process heavy files in the background without two workers fighting over the same job. | Low-priority bounded workers with `FOR UPDATE SKIP LOCKED`, retry/cooldown tracking, and no cloud/provider calls by default. | shipped | 100% | Deferred workers claim jobs, track retries/cooldowns, and use explicit terminal states. | Extend queue families for heavy extractor stages in V2.8. | Extend queue families for heavy extractor stages in V2.8. |
| Duplicate and version suppression | P0 | Avoid showing the same document repeatedly while keeping all evidence recoverable. | Duplicate suppression by content hash, conservative same-document/version-family suppression, and semantic near-duplicate grouping in retrieval. | in progress | 75% | Exact duplicate suppression, conservative path/title version-family suppression, and advisory semantic duplicate clusters for corpus chunks, episodes, and claims exist. Semantic clusters choose canonical members, suppress noncanonical retrieval siblings, and expose sanitized counts without automatic deletion. | Calibrate semantic thresholds from live review feedback, then feed clusters into V3 evaluation and automation-first librarian workers. | Calibrate semantic thresholds from live review feedback, then feed clusters into V3 evaluation and automation-first librarian workers. |
| Corpus retrieval | P0 | Let file contents participate in search alongside session memories. | Full-text, fuzzy, pgvector chunk embeddings, trust rank, freshness, deletion state, and canonical duplicate filtering. | shipped | 100% | Corpus retrieval combines these signals, including freshness reranking, and now requires `indexed` source assets while suppressing deleted, non-indexed, and non-canonical duplicate assets. | Keep query-aware snippets and retrieval explainability aligned with live feedback. | Use live retrieval feedback for targeted ranking improvements. |

## V2.6: Mail Capture And Runtime Configuration

| Piece | Priority | Plain-English Purpose | Roadmap Intent | Status | Progress % | Current Evidence / Remaining Gap | Remaining Work | Queued Next |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Runtime settings catalog | P1 | Let operators see and change settings safely from the app. | Settings catalog-backed runtime settings with dashboard editing, environment override visibility, masked secrets, audit events, and confirmation-gated apply actions. | shipped | 100% | Settings catalog, dashboard editing, masking, audit events, and apply coordination exist. | Keep new settings catalog-backed and avoid implying Windows Registry usage. | Keep new settings catalog-backed and avoid implying Windows Registry usage. |
| Terminology cleanup | P3 | Avoid confusing users into thinking settings are stored in the Windows Registry. | Public docs and dashboard wording should not imply Windows Registry usage. | shipped | 100% | Settings are documented as cross-platform catalog definitions plus local PostgreSQL overrides. | Preserve this wording in future settings docs. | Preserve this wording in future settings docs. |
| Dashboard forms | P1 | Make setup and changes guided instead of requiring manual API calls. | Dashboard forms for settings edits, mail profile creation, Gmail OAuth setup, confirmation-gated apply actions, validation errors, and mail/token status. | shipped | 100% | Dashboard forms exist for settings, OAuth, validation, status, and source-aware mail profiles that keep IMAP account/server fields off Outlook COM setup and expose Outlook child-folder inclusion plus incremental basis selection. | Maintain provider-specific defaults and copy as mail sources or policies change. | Maintain provider-specific defaults and copy as mail sources or policies change. |
| Dashboard delivery | P1 | Make the dashboard the normal operator experience, with raw JSON kept for debugging. | React/Vite operations console served by FastAPI at `/dashboard`, with raw JSON only in developer/debug contexts. | shipped | 100% | FastAPI serves the bundled dashboard; raw JSON is diagnostic rather than primary monitoring UI. | Keep primary operator flows in the dashboard, not raw JSON panels. | Keep primary operator flows in the dashboard, not raw JSON panels. |
| Production deployment | P1 | Keep installed runtime data separate from the repo so updates are safer. | Production PC deployment under `D:\FluxLLMKB` with repo-independent app, private, data, logs, runtime, and backup directories plus install/update/start/stop/status scripts. | shipped | 100% | Production scripts and separated runtime layout exist under a configurable install root; update/closeout now regenerate production Compose with optional or required Docker GPU passthrough, bounded local vision/ASR environment settings, and API/worker overrides that keep container roots on `/app`, `/app/private`, and `/app/logs` while host launchers keep Windows paths. Docker build and host pip install stages have explicit timeouts, retry/timeout knobs, optional apt mirror overrides for slow Debian routes, and an optional package-index override for large GPU wheels on slow routes. | Keep closeout/deploy scripts aligned with layout changes, and verify GPU probes after driver, Docker, apt mirror, package-index, or base-image updates. | Keep closeout/deploy scripts aligned with layout changes and GPU runtime checks. |
| Docker control plane | P1 | Run the main services predictably while host-only actions stay outside Docker. | Docker-hosted PostgreSQL/pgvector, FastAPI, REST APIs, dashboard assets, IMAP worker, crawler, and normal extraction workers. | shipped | 100% | Docker hosts the normal Flux API/dashboard/worker runtime. | Keep host-only actions routed through host agent. | Keep host-only actions routed through host agent. |
| Gmail OAuth and IMAP capture | P1 | Pull Gmail/IMAP messages into Flux with visible auth and sync state. | Gmail installed-client OAuth, token refresh before IMAP XOAUTH2 login, token health reporting, blocked auth states, TLS IMAP capture, UID cursors, optional IDLE, and reconciliation. | in progress | 87% | Gmail OAuth, profile-scoped IMAP capture, token health, blocked auth states, scheduler reconciliation, stale claimed/running sync recovery, worker-startup orphan recovery, and due-backoff manual retry exist. Broader live-provider validation should continue across Gmail and standards-compliant IMAP servers. | Broaden live-provider validation and keep auth-required/expired/stale states actionable. | Broaden live-provider validation and keep auth-required/expired/stale states actionable. |
| Mail post-processing defaults | P1 | Keep captured mail organized without deleting anything unless explicitly allowed. | Safe defaults to move/remove capture label or move to processed folder; permanent trash/delete opt-in and confirmation-gated. | shipped | 100% | Provider-specific policies exist for Gmail labels/trash and generic IMAP copy/delete/expunge, Outlook COM setup defaults to `none`, unsupported Outlook COM mailbox mutations are blocked, and dry-run/audit/dashboard visibility exist with destructive-action confirmation. | Continue live-provider validation across Gmail and standards-compliant IMAP servers; keep Outlook COM post-processing disabled until a safe COM mutation design is explicitly scoped. | Continue live-provider validation across Gmail and standards-compliant IMAP servers; keep Outlook COM post-processing disabled until a safe COM mutation design is explicitly scoped. |
| Outlook COM catch-up | P2 | Let Windows Outlook users import selected folders through a separate host process. | Classic Outlook COM catch-up for selected mailbox folder paths, with a separate Windows host process, heartbeat, blocked-state reporting, and request claiming. | in progress | 96% | Separate Outlook host process model, dashboard status, source-aware profile setup, null account/server persistence, manual-sync defaults, include-subfolders option defaulting on for Outlook profiles, recursive selected-folder traversal, max-message cap enforcement during COM export, incremental per-folder COM cursors with `ReceivedTime` default and `LastModificationTime` moved-mail mode, ascending timestamp traversal, safe cursor checkpoints after successful exports and known duplicates, no-cursor EntryID duplicate skipping, per-folder stop-on-export-failure without cursor over-advance, blocked unsupported post-processing, PostgreSQL-safe request claim SQL, stale heartbeat reporting, process-ID heartbeats, single-host lock enforcement, active heartbeat refresh during long COM exports, pending request cancellation, stale claimed-request recovery after dead hosts without a claim-age delay, host-side `/app/private` spool mapping, Docker/container-safe `/app/private` path resolution, background Docker-side spool indexing jobs, rich body/HTML/attachment export after programmatic-access validation, deterministic rich `.eml` boundaries and `mail spool-dedupe` safe duplicate purge/reporting, and host loop exception hardening exist. | Validate deployed Outlook catch-up across more selected-folder shapes, blocked dependency states, moved-mail workflows, post-reclaim duplicate-heavy reruns, and large-folder live runs; keep mid-execution cancellation as explicit conflict reporting unless a safe COM interruption design is scoped; add isolated/time-bounded COM export only if programmatic-access prompts recur. | Validate recursive incremental Outlook request completion on the Windows host and keep the programmatic-access requirement visible in operator setup guidance when manual docs are next requested. |
| Mail dashboard controls | P1 | Let operators start, inspect, and troubleshoot mail sync without leaving the dashboard. | Dashboard controls for IMAP worker state, Outlook COM host state, per-profile schedule fields, manual sync requests, last sync, next sync, backlog, and errors. | shipped | 100% | Dashboard exposes mail worker state, scheduler counts, profile sync controls, Outlook state, source-aware schedule fields, Outlook include-subfolders setup, backlogs, errors, post-process policy fields, dry-run, recent post-process outcomes, Outlook COM request rows with pending cancellation plus claimed/running conflict feedback in Jobs, and background corpus sync job progress/cancel controls. | Add deeper per-run debug detail only when operator workflows need it. | Add deeper per-run debug detail only when operator workflows need it. |
| Profile-scoped OAuth | P1 | Keep each mail account's login status separate and understandable. | Profile-scoped Gmail OAuth actions and status for multiple IMAP/Gmail accounts. | shipped | 100% | Multiple profile-scoped OAuth controls and status exist without floating global OAuth controls. | Keep OAuth diagnostics profile-scoped. | Keep OAuth diagnostics profile-scoped. |
| Unified private mail spool | P0 | Prevent partially exported mail from being indexed before it is complete. | Unified private spool for IMAP and Outlook exports; Flux indexes only `ready` and ignores `_inflight`. | shipped | 100% | IMAP and Outlook exports use a private spool with `_inflight` and `ready` states; the searchable corpus indexes manifest metadata, stores managed `body.txt` and attachment chunk text in private disk sidecars rather than PostgreSQL plaintext bodies, leaves raw backups plus duplicate HTML as private disk artifacts, and blocks sync when the configured mail root is not accessible from the current runtime. | Keep public docs free of raw mail, spool data, and local runtime values. | Keep public docs free of raw mail, spool data, and local runtime values. |
| Consumer access | P0 | Let other tools read Flux knowledge without giving them write access. | Read-only lookup endpoints for REST, MCP, and CLI consumers, including search, brief, asset lookup, and chunk lookup. | shipped | 100% | REST/CLI/MCP consumer search and brief access plus corpus asset/chunk lookup exist. | Maintain compatibility while adding explainability fields. | Maintain compatibility while adding explainability fields. |

## V2.7: Mail And Retrieval Production Hardening

| Piece | Priority | Plain-English Purpose | Roadmap Intent | Status | Progress % | Current Evidence / Remaining Gap | Remaining Work | Queued Next |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Search result actions and previews | P1 | Make search results immediately useful by opening the right file, mail, or preview. | Search results should open useful logical representations, including sanitized mail views and host-agent file actions. | shipped | 100% | Dashboard search result actions, sanitized in-app mail viewing, file text previews, host-agent open/reveal actions, copy path, action states, and related-evidence grouping exist. | Keep host-agent validation/audit strict as more file actions are added. | Keep host-agent validation/audit strict as more file actions are added. |
| Logical related evidence | P1 | Show attachments, archive contents, and sidecars together so evidence stays understandable. | Archive members, mail attachments, embedded objects, and sidecars should appear under parent logical results. | in progress | 83% | Canonical mail body and attachment spool files with private content sidecar refs, archive/container children, known child assets, embedded media sidecars, and embedded parser results for documents, diagrams, images, media, subtitles, mail exports, calendar/contact files, structured data, reports, SQLite schema metadata, and metadata-first domain formats are grouped as related evidence. Broader embedded media and proprietary local-tool extraction still need live validation. | Extend related-evidence grouping as deeper embedded media and specialized local-tool extraction lands. | Extend related-evidence grouping only where new embedded extractors produce useful sanitized metadata or chunks. |
| Lock-tolerant indexing and cloud-sync coexistence | P0 | Keep indexing from breaking when files are open, syncing, or still being written. | Read-only/shared access, temporary extraction snapshots, no exclusive file ownership, cloud-sync edge handling, optional VSS, and compatibility tests. | in progress | 84% | Watcher stability gating, `pending_stable`, `retrying_locked`, `blocked_locked`, retry/cooldown, VSS settings/capability reporting, scoped reliability evidence, and all-root readiness summaries exist. Actual VSS snapshot extraction and broad cloud-sync compatibility proof remain. | Validate OneDrive/SharePoint/Dropbox, open Office files, large writes, save/rename patterns, and all-root readiness on real workloads; implement opt-in VSS only if evidence still requires it. | Use the all-root reliability gate to collect live evidence before implementing VSS extraction. |
| Mail post-process hardening | P1 | Make mailbox cleanup predictable and retry-safe after messages are captured. | Provider/profile-specific mailbox actions, explanatory dashboard copy, dry-run/audit records, and retry-safe handling. | shipped | 100% | Explicit policies now cover no-op, Gmail remove label, Gmail move label, Gmail trash, generic IMAP move, confirmation-gated delete/expunge, and blocked Outlook COM non-`none` policies without issuing IMAP commands. Sync records post-process events, surfaces failures, preserves exported spool data, and avoids advancing the cursor past failed UIDs. | Continue live-provider validation and keep raw mail content out of audit views. | Continue live-provider validation and keep raw mail content out of audit views. |
| Retrieval snippets and explainability | P0 | Show why a result appeared so users can trust or tune search. | Query-aware snippets, highlighted terms, retrieval streams, raw ranks, source trust, freshness, duplicate/version suppression, lifecycle penalties, and configurable filters. | in progress | 85% | Query-aware snippets, highlight ranges, search-result explanation metadata, per-query retrieval filters, filter-exclusion traces, sanitized exact/version/semantic suppression metadata, confidence bands, deprioritization diagnostics, retrieval stream timing/plan diagnostics, and brief-packing traces are exposed through REST, MCP, CLI, and the dashboard. Automated-action rationale remains planned. | Add deeper explanation for escalation and automated lifecycle actions after V3 calibration evidence has enough live feedback, and use live timing evidence to prioritize ranking/query-plan fixes. | Add deeper explanation for escalation and automated lifecycle actions after V3 calibration evidence has enough live feedback, and use live timing evidence to prioritize ranking/query-plan fixes. |
| Scheduled sync and worker reliability | P1 | Make recurring mail sync observable, claim-safe, and recoverable after missed runs. | First-class IMAP scheduler state machine with claimed/running/completed/failed runs, drift, retry cooldown, auth blocks, ownership, missed-run reconciliation, and tests. | shipped | 100% | Claimable IMAP scheduled sync runs, lifecycle state, run history, drift/missed-run fields, owner/attempt metadata, PostgreSQL-safe claim SQL, stale claimed/running expiry into retryable backoff, worker-startup orphan recovery for interrupted deployments, due-backoff manual retry, health diagnostics, and dashboard scheduler counts are visible. | Continue live-provider validation for tight intervals across Gmail and standards-compliant IMAP providers. | Continue live-provider validation for tight intervals across Gmail and standards-compliant IMAP providers. |
| Error diagnostics and operator UX | P1 | Turn failures into actionable messages instead of vague errors. | Standard API error envelopes and dashboard alerts with code, severity, component, target metadata, retryability, user action, technical detail, and links. | in progress | 90% | Structured API error envelopes, dashboard actionable diagnostics, expandable details, copyable JSON, navigation targets, row-level diagnostic panels for retrieval, watcher, workers, slow/blocked jobs, stale mail scheduler runs, stale Outlook host state, Outlook request cancellation conflicts, and mail evidence, plus `POST /api/diagnostics/actions`, CLI/MCP remediation, audit events, and `settings_mutated: false` recovery responses exist. | Validate remediation against live recurring failures, add richer recovery links only where evidence shows repeated operator need, and keep automatic settings mutation out of scope. | Use live diagnostic outcomes to decide the next recovery links or automated-action rationale work. |

## V2.8: Indexer Acceleration And Local Inference Optimization

| Piece | Priority | Plain-English Purpose | Roadmap Intent | Status | Progress % | Current Evidence / Remaining Gap | Remaining Work | Queued Next |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Acceleration lane | P1 | Make large folder indexing faster while showing what is slowing it down. | Make high-volume indexing fast and predictable on one PC without requiring GPU or heavyweight media tooling. | in progress | 92% | V2.8 foundation now exposes acceleration status, permanent cache layout, explicit watcher backend policy/probe, worker-family queues, worker caps, backpressure/debug rows, duration telemetry, OCR cache hit/miss telemetry, ASR cache hit/miss plus segment telemetry, recursive container member counters, local vision cache counters, decorative-image skips, frame sample counts, thumbnail cache telemetry, parser/cache telemetry, embedding refresh telemetry, bounded crawler hash parallelism, incremental scan manifest skip counters, durable benchmark history through REST, CLI, MCP, and the dashboard Performance tab, and benchmark scenarios for reliability, host/cloud calibration, cache readiness, and tuning. Cache-backed local OCR, bounded OCR preprocessing, local ASR, recursive containers, optional configured local loopback, Docker host-gateway, or Docker Compose Ollama inference for vision, production GPU mode with Docker passthrough, CUDA ASR settings, optional ONNX Runtime status detection that is not installed or required by default, scene-transition video sampling, local deterministic embedding refresh jobs, native watcher proof, scan/soak/watcher benchmark modes, real-root aggregate calibration, model-readiness benchmark mode, labeled before/after comparison fields, manual recommendation candidates, decoupled watcher liveness, queued watch-event sync work, stale root-sync recovery, pre-recursion exclude pruning, path-batched watch sync jobs, and aggressive local worker/hash defaults are implemented slices. | Continue throughput tuning with live all-root evidence, validate Docker Ollama GPU behaviour and memory ceiling on real blocker backfills, and keep provider-specific acceleration beyond local Docker services gated by evaluation controls. | Validate the Docker Ollama GPU-backed local vision blocker backfill, then continue throughput tuning with live all-root evidence. |
| Hardware capability detection | P1 | Show which local hardware and tools Flux can use before a job gets stuck. | Detect CPU, memory pressure, disk hints, NVIDIA/CUDA, ONNX Runtime providers, DirectML/OpenVINO, and local model servers; expose capability and blocked-runtime reasons. | in progress | 85% | CPU count, Windows memory when available, cache-root disk space, NVIDIA `nvidia-smi`, Docker GPU passthrough fail-fast checks in required GPU mode, production Compose GPU requests for API/worker/Ollama containers, Docker-visible memory and Postgres `/dev/shm` reporting in production status, ONNX Runtime provider reporting with import-failure containment, watchdog availability, disabled-by-default local-model probing, Docker host-gateway and Compose Ollama local model URLs, and model/tool readiness benchmark telemetry are reported or configured. DirectML/OpenVINO-specific policy and model routing remain planned. | Validate live NVIDIA, CTranslate2 CUDA, Docker memory ceiling, high-memory Postgres pressure, and Docker Ollama local-model probes after deployment, then extend provider-specific routing only when a worker family needs it and local evidence justifies the extra surface. | Verify deployed CUDA/ASR, Docker memory, Postgres shared-memory sizing, and Docker Ollama local-model status before adding provider-specific routing. |
| Permanent cache and model layout | P1 | Keep expensive local work reusable across app updates. | Keep dependency, model, OCR, ASR, vision, thumbnail, parser caches, generated sidecars, and private mail-content sidecars persistent across deploy updates without relying on container image layers. | in progress | 90% | Cache root resolution and named directories for models, OCR, ASR, vision, thumbnails, parser output, embeddings, private mail content sidecars, and temp files are visible in status/dashboard. Production Compose now uses Docker named volumes for container-owned PostgreSQL data, cache, data, runtime, logs, and Ollama model blobs, with a guarded migration script that dumps/restores legacy PostgreSQL bind data and copies existing cache/runtime/log/model directories into empty volumes while leaving host data for rollback. OCR jobs store redacted Tesseract cache entries under the OCR cache, ASR jobs store redacted faster-whisper cache entries under the ASR cache, managed mail body/attachment plaintext is kept in private sidecar files instead of PostgreSQL chunk bodies, and embedding refresh records source hashes/cache keys in vector metadata without raw text. These families expose hit/miss telemetry where implemented; broader model lifecycle remains planned. | Validate Docker-volume migration and persistent Docker Ollama model reuse during production rollout, extend cache records and hit/miss telemetry to parser output and future model-backed providers as those worker families land, and validate mail sidecar retention/repair. | Validate Docker-volume migration and persistent Docker Ollama model reuse, then extend cache records and hit/miss telemetry to parser output and future model-backed providers as those worker families land. |
| Resource-aware worker scheduling | P0 | Prevent one kind of heavy work from starving everything else. | Split queues by job family and locality with concurrency caps, priorities, rate limits, backpressure, cooldowns, and time budgets. | in progress | 91% | Corpus jobs carry fixed worker-family, resource-class, priority, and time-budget metadata; existing `--kind` filters and broader aliases such as `data`, `mail`, `reports`, and `metadata` claim by family; configured `acceleration.worker_cap.*` values cap concurrent family claims by ranking candidates per family and claiming no more than the remaining cap; completion/retry/failed/block paths record duration telemetry; repeated non-lock failures now stop at terminal `failed` after `worker.failure_max_attempts`; worker/backfill claims use unique worker-instance ids with matching runtime heartbeats so stale `running` recovery is not masked by restarted aggregate component names; status surfaces show cap usage, `over_cap_running`, worker-family backpressure, oldest pending age, retry/lock transitions, sanitized slow-job rows, parser cache telemetry, practical parser counts, and `manifest_skipped_unchanged` counters. Benchmark `soak` mode creates benchmark-tagged synthetic jobs by worker family, claims them through the same cap logic, records cap/backpressure/duration telemetry, and purges them. `run_corpus_backfill(workers=N)` now processes claimed jobs through a bounded thread pool, preserves claimed-job caps, and converts per-job failures into independent retry/block/fail/complete accounting. Local defaults now start at text 8, office/archive 3, GPU-heavy families 2, general 4, hash parallelism 8, and worker batch size 24 while staying setting/env overrideable. | Validate the worker-instance stale recovery and aggressive defaults on live all-root runs after deployment interruption scenarios, then adjust only from measured disk, DB, and worker-family pressure. | Use synthetic soak plus real observed telemetry to tune caps only from hardware evidence, and verify interrupted-deployment recovery on live watcher/worker roots. |
| OCR, image, diagram, and vision acceleration | P2 | Make images and diagrams searchable locally while avoiding unnecessary or unsafe work. | Prefer structural extraction before OCR; add image hash caches, decorative skips, batching, thresholds, language routing, and local provider chains. | in progress | 80% | Image jobs and image-only PDFs can use local Tesseract OCR with `pdftoppm` rendering for PDFs, page caps, 200 DPI plus a 6000-pixel render edge cap, native-image downscaling above 6000 pixels, redacted source-keyed cache reuse, explicit `blocked_missing_dependency` states, OCR timeout classification as `blocked_timeout`, and cache hit/miss plus preprocessing telemetry. Decorative-image skips avoid work for spacer assets and small inline icons before OCR or vision, including strict-root no-content completion. Empty OCR results escalate to configured local vision before strict roots complete as no-content. OCR+vision and transcript+frame-vision chunk streams are reindexed sequentially before persistence. Optional local vision descriptions use configured local loopback, Docker host-gateway, Docker Compose Ollama inference, or the loopback-published Docker Ollama port used by Windows host-agent roots when `acceleration.vision.enabled` and `acceleration.vision.model` are configured; production GPU mode enables bounded Qwen local vision through Docker Ollama, submits scaled 1280-pixel vision copies with a larger answer budget, records Qwen thinking-field fallback when needed, stores redacted non-empty results in the vision cache, and keeps failed model attempts visible through image-job status/error telemetry. Cloud OCR/vision remains off by default; language routing and provider-specific batching remain planned. | Validate live host-agent and worker local-vision blocker backfills after Docker Ollama GPU deployment before adding language routing or provider-specific batching; tune OCR timeout/page policies only from repeated live evidence. | Requeue/backfill image blockers through configured Docker Ollama GPU local vision, then add language routing/provider batching only after evaluation controls are ready. |
| Audio/video transcription acceleration | P2 | Reuse transcripts and local speech tools so media becomes searchable without cloud transcription. | Reuse sidecar transcripts, then run local deferred transcription with ffmpeg/ffprobe or bundled equivalents and faster-whisper/CTranslate2. | in progress | 73% | Sidecars are recognized as targets; local ASR now probes media with `ffprobe`, respects `acceleration.asr.max_duration_seconds`, extracts bounded mono 16 kHz temp audio through `ffmpeg`, transcribes with faster-whisper from `acceleration.asr.model_path`, passes configured `acceleration.asr.device` and `acceleration.asr.compute_type` to `WhisperModel`, writes redacted ASR cache entries, and reports segment/cache telemetry. Production GPU mode sets CUDA/float16 for faster-whisper/CTranslate2 when the GPU runtime is available. Video jobs can optionally use `acceleration.video.frame_sampling.enabled`, `acceleration.video.scene_threshold`, and `acceleration.video.frame_sample_count` for scene-transition sampling into the thumbnail cache. Media diagnostics now include sidecar use, ASR segment totals, frame sample counts/timestamps, thumbnail cache counters, stale-lock evidence, and blocked dependency reasons where available. | Validate live CUDA ASR model execution, then add progress reporting and semantic media backfill. | Verify deployed CTranslate2 CUDA visibility, then add progress reporting and semantic media backfill. |
| Embedding and vectorization throughput | P1 | Keep search vectors fresh without redoing unchanged work. | Batch embeddings by model/provider/hardware target, support optional accelerated providers, and bulk upsert vectors. | in progress | 78% | Deterministic local `flux-hash-v1` embeddings now use a provider boundary, source-hash metadata, `corpus_embed` jobs, CLI/REST/MCP status/enqueue/backfill surfaces, indexed-only corpus candidate selection, stale non-indexed chunk/vector repair, and worker-family telemetry for vectors, skipped unchanged items, batches, and cache hits/misses. Optional accelerated providers remain planned. | Validate stale vector repair on live roots and add provider-specific accelerated backends only after evaluation controls and dimensionality migration policy are designed. | Add provider-specific accelerated backends only after evaluation controls and dimensionality migration policy are designed. |
| Local model-assisted knowledge optimization | P2 | Use local models later to suggest better memory cleanup without sending data away. | Use local model backends only when available to enrich memory governance and indexing decisions. | planned | 10% | Local inference routing is not yet implemented as a shared provider layer for knowledge optimization. | Let optional local Llama/Gemma-class models assist librarian-worker proposals, semantic clustering, contradiction checks, canonical-summary drafts, and audit rationale generation without remote calls; fall back cleanly to rule-based behavior when unavailable. | Let optional local Llama/Gemma-class models assist librarian-worker proposals, semantic clustering, contradiction checks, canonical-summary drafts, and audit rationale generation without remote calls; fall back cleanly to rule-based behavior when unavailable. |
| Native and incremental filesystem performance | P0 | Make repeated scans and file watching cheaper on large folders. | Evaluate native watcher backends, incremental scan manifests, prefilters, content-hash caches, bounded parallel hashing, and temporary snapshots. | in progress | 96% | `watcher.backend`, metadata-only scan manifests, bounded hash parallelism, strict-root metadata-only blockers, real-root benchmark scope, reliability/host-cloud scenarios, indexer reliability gate, and all-root reliability matrix now summarize file-churn, retry/block, watcher reconciliation, worker/watcher evidence, scoped benchmark freshness, and cloud-sync delayed-availability evidence without private paths or settings mutation. Service and host-agent watcher loops now run independent heartbeat threads with busy/stage/loop-duration/event-count/queue-depth metadata, keep heartbeats fresh during startup/periodic reconciliation, survive transient host-agent database restart/write failures without killing background threads, apply effective include/exclude globs before emitting watch events, enqueue coalesced background sync jobs for watch events instead of running `sync_corpus` inline, batch pending path-triggered syncs and Outlook export deltas, create follow-up jobs instead of mutating active running syncs, recover stale running root syncs using progress heartbeats while same-root root-sync claims are guarded, prune excluded directories before crawler/watcher descent, and avoid watchdog stability snapshots when no pending stable files exist. Dedicated worktrees under watched repositories are excluded by default. | Run all-root reliability on live high-volume roots after deployment, compare scoped benchmark history after deployments, validate stale root-sync recovery and path-batch behaviour under real watcher and Outlook churn, clear strict-root blockers, and keep VSS/provider acceleration blocked until evidence justifies it. | Keep validating live high-volume roots through the all-root reliability gate before VSS snapshot extraction or provider-specific acceleration. |
| Observability and benchmarks | P0 | Give operators before/after evidence instead of guessing which setting or update helped. | Dashboard panels and benchmark fixtures for throughput, latency, cache hits, model warm/cold state, CPU/GPU mode, blocked dependencies, slow files, and p50/p95 indexing times. | in progress | 98% | Dashboard Performance and Jobs show acceleration capabilities, selected watcher backend, cache root, worker-family queue counts, cap/backpressure status, p95 duration where available, OCR/ASR/container/parser/embedding telemetry, manifest skip counters, background root-sync stage/path/file/job telemetry, lock owner/progress heartbeat details for corpus jobs, a Jobs Progress column and detail panel fields, top skipped-directory scan telemetry, slow-job diagnostics, stale mail scheduler diagnostics, split API/host database health, durable benchmark history, reliability evidence, strict-root metadata-only blockers, and all-root reliability matrix. Mail panels now distinguish mail/runtime failures from corpus extraction failures under mail-spool paths. Code diagnostics now live under Retrieval and operational diagnostics live under Diagnostics. Benchmark run modes cover `scan`, `soak`, `watcher`, `model`, and `all`; scenario runners cover `standard`, `reliability`, `host_cloud`, `cache_readiness`, and `tuning`, return `diagnostics[]`, keep `settings_mutated: false`, and expose manual `recommendations.candidates[]`. Retrieval explain payloads now include stream timing and plan metadata for lexical, fuzzy, vector, sidecar, symbol, and hydration stages. The standard retrieval suite now resolves expected evidence by exact source path, includes root-level and nested fixtures, refreshes suite-local semantic duplicate evidence, and runs filtered explain paths before result truncation. Stored benchmark records are metadata only and exclude raw text, private watched roots, mail contents, credentials, and embeddings. | Compare benchmark history across deployment updates, tune defaults from observed hardware evidence, clear real strict-root blockers before go-live, validate split database health, live root-sync recovery, Outlook path-batch progress, and retrieval latency after PostgreSQL compose tuning is deployed, and keep automatic settings mutation out of scope. | Use all-root reliability, retrieval code diagnostics, and operational diagnostics as the operator evidence layer; do not add automatic settings mutation. |

## V3: Scale And Evaluation

| Piece | Priority | Plain-English Purpose | Roadmap Intent | Status | Progress % | Current Evidence / Remaining Gap | Remaining Work | Queued Next |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Code-aware corpus indexing | P0 | Help Codex answer code questions by understanding symbols, files, tests, and relationships. | Parser-backed code intelligence over opted-in repositories so Codex can find files, symbols, definitions, references, tests, routes, handlers, and implementation locations without treating code only as generic text. | in progress | 93% | Code-like files produce parser-aware `asset_chunks`, durable `code_symbols`, and `code_references`; Python tests/fixtures/decorators/inheritance, JS/TS exports/routes/callers, C# namespaces/controllers/routes/tests/calls, frontend markup components/events/form actions/selectors, stylesheet selectors/custom properties/keyframes/media queries, SQL migrations, config files, notebooks, generated markers, and conservative Go/Rust/PowerShell symbols are covered. Code-heavy synthetic fixtures now exercise 25 public-safe files across backend, frontend, markup, style, SQL/config, and test paths. Code/corpus filters include `relationship`, `path_glob`, `include_generated`, and logical-kind prefiltering before explain-result truncation; exact-symbol/path ranking, generated suppression, privacy-safe status/search/symbol lookup, feedback-derived gaps, benchmark-derived code gaps, and dashboard code diagnostics/search inputs exist. Broad search, brief, and explain now exclude code unless the query carries `file_kind=code` / `filters={"file_kinds":["code"]}` as the only file-kind request; mixed code plus non-code file-kind filters are rejected, while dedicated code search/symbol lookup remain available. | Continue validating explicit-code and non-code retrieval on live opted-in repositories, tune repeated benchmark/live misses, add deeper parser drill-downs only where value is proven, and keep full IDE/static-analysis replacement out of scope. | Use benchmark/live feedback for targeted parser/ranking tuning; keep public docs/tests private-content-free. |
| Historical Codex backfill | P2 | Safely import useful older Codex work without pulling in unsafe or noisy history. | Historical Codex backfill with redaction. | in progress | 35% | Approved `codex_backfill` review jobs can now be ingested in bounded batches from `.json`, `.jsonl`, `.md`, and `.txt` sources with redaction, duplicate source-hash skips, blocked missing-source status, sanitized provenance, and audit-visible ingestion outcomes. Broad historical discovery and automatic import remain future work. | Validate with real approved backfill batches, add only the source discovery and review staging needed for older Codex exports, and keep raw transcript persistence, automatic import, and unreviewed history ingestion out of scope. | Validate with real approved backfill batches, add only the source discovery and review staging needed for older Codex exports, and keep raw transcript persistence, automatic import, and unreviewed history ingestion out of scope. |
| Retrieval benchmarks | P0 | Measure whether Flux search is actually finding the right things. | Retrieval benchmark suite. | in progress | 92% | The deterministic synthetic suite now seeds temporary public-safe retrieval cases, calls search/explain/brief paths, records metadata-only history in `retrieval_benchmark_runs`, and exposes REST, CLI, MCP, dashboard surfaces, and operational diagnostics aligned to the current `metrics`/`case_results` schema. Metrics include top-1 accuracy, precision@3, recall@5, MRR, nDCG@5, brief recall, brief dilution, scope pass counts, suppression pass counts, elapsed time, metric deltas, confidence-band summaries, semantic-threshold advisory candidates, and sanitized case failures for mail/file filters, current-only, lifecycle-deprioritized, semantic duplicate/guardrail, code-symbol misses, callers, tests, routes, generated-file handling, config/migration lookup, fallback recovery, and cross-root disambiguation. The standard suite now avoids unrelated expected-result fallback, covers root-level fixtures, refreshes semantic duplicate clusters for fresh synthetic roots, passes normalized filters into explain-time raw retrieval, and validates source-path, route, test, generated, episode, suppression, expected code-symbol, exact-definition ranking, broad non-code policy prompts, brief packing, Codex hook fallback, and dashboard search-focus payload cases without automatic settings mutation. Suppression checks now target expected hits so unrelated duplicate runner-ups do not create false failures. The `governance-shadow` suite now adds metadata-only stale, apply/recover, stale-proposal conflict, duplicate-cluster, capture-ingestion, feedback-gap, contradicted, low-confidence, protected/current, and false-positive guardrail cases plus proposal counts, categories, guardrail pass/fail counts, precision-style summaries, and `settings_mutated: false`. | Continue using benchmark/live feedback for ranking/parser tuning and live governance calibration. Do not mutate settings, thresholds, ranking, retention policy, semantic clusters, or lifecycle state automatically from benchmark output. | Use benchmark/live feedback for targeted parser/ranking tuning and to keep governance apply gates calibrated. |
| Guarded operator automation | P2 | Remove repetitive safe dashboard actions while preserving human control over risky work. | Default-off, allowlisted automation for evidence refresh, approved capture ingestion, safe diagnostic recovery, embedding refresh, and governance shadow proposals, with durable audit history and no settings mutation. | in progress | 55% | `operator.automation.*` settings, durable `operator_automation_*` run/action history, REST/CLI/MCP `automation status|run|actions`, worker cadence integration, dashboard Automation tab, sanitized evidence, manual-required denylist, and `settings_mutated: false` contracts exist. | Validate cadence, cooldowns, wording, and action usefulness on live private workloads; keep deletes, OAuth, host startup, restart/reindex settings, capture approve/reject, high-risk governance, file open/reveal, and ambiguous actions manual. | Use Automation audit history and Overview recommendations to tune only allowlisted low-risk actions after live evidence. |
| Optional search backend | P3 | Consider a stronger search engine only after the current baseline is measured. | Optional ParadeDB/BM25 path. | planned | 0% | Not started. | Evaluate only after baseline retrieval benchmarks exist. | Evaluate only after baseline retrieval benchmarks exist. |
| Librarian workers | P2 | Let Flux suggest cleanup candidates so memory does not become stale or cluttered. | Automation-first local librarian workers for derived-memory cleanup, consolidation, and linting. | in progress | 62% | Governance runs now generate sanitized proposals from retention quality, claim lifecycle state, active semantic duplicate clusters, capture-ingestion outcomes, code feedback summaries, and the latest governance-shadow evidence. The corpus worker can schedule default-off librarian runs on cadence and stays shadow-only unless explicit settings enable auto mode and low-risk auto-apply. | Calibrate cadence and thresholds from live runs, broaden source-specific proposal quality, and keep auto-apply limited to benchmark-gated low-risk claim lifecycle actions. | Use live governance digests and skipped/conflict telemetry to tune proposal quality before expanding automation. |
| Automated memory governance | P2 | Allow safe routine cleanup only after tests prove it will not damage useful memory. | Policy-gated automation for routine memory lifecycle optimization. | in progress | 68% | `memory_governance_*` persistence, REST/CLI/MCP surfaces, dashboard Review panels, benchmark-gated apply/recover, stale-proposal conflict detection, protected-memory blocks, low-risk auto-apply allowlist, audit events, policy snapshots, and sanitized digests now exist. Hard delete and runtime settings mutation are not governance actions. | Validate against live private workload evidence, tune protected-memory rules and precision thresholds, and add deeper target-specific mutation for semantic cluster presentation only after recovery UX is proven. | Keep routine actions reversible and benchmark-gated; do not add hard deletes, broad contradiction automation, or settings mutation. |
| Local consolidation and escalation | P2 | Combine obvious duplicates locally and ask a person only for risky decisions. | Local-only consolidation with rare human escalation. | in progress | 25% | Active semantic duplicate clusters now feed `semantic_cluster_apply` and `canonical_cluster_promote` governance proposals with manual guardrails, risk scoring, and recovery visibility. Optional local-model rationale settings exist with deterministic fallback, but local-model-assisted consolidation remains mostly future work. | Use local model-assisted consolidation only after live governance evidence shows stable precision; keep protected memories, canonical promotion, contradictions, destructive changes, and high-impact low-confidence decisions manual. | Expand canonical-summary drafts and escalation policy only after governance proposal precision is proven. |
| Operator digests | P2 | Summarize what automation did or wants to do so the operator stays in control. | Periodic reporting for automated memory governance. | in progress | 70% | Governance runs persist bounded local digests with new proposal counts, blocked proposals, recoverable actions, benchmark gate state, and recommended manual next actions. REST, CLI, MCP, and the Review tab expose digest, guardrail, action, telemetry, and recovery views without outbound delivery. | Tune digest retention and operator wording from live usage; keep outbound email, webhooks, and notifications out of scope until local review workflows are stable. | Use digest evidence to prioritize manual review and recovery, not to bypass confirmation. |

### Future Slice: Code-Aware Corpus Indexing

Status: foundation bundle `implemented`; broader code-aware indexing remains
`in progress`. This is a cohesive implementation slice, separate
from V2.8 indexer reliability and benchmark history. It should use the V2.8
watcher backend policy, worker-family scheduling, crawl manifests, parser cache
telemetry, and synthetic benchmark history as foundations, but it should not
implement VSS extraction, provider-specific embedding backends, runtime tracing,
or V3 retrieval/governance benchmarks as part of the same slice.

Plain-English overview: this foundation helps Codex answer questions
about codebases by understanding files, symbols, tests, routes, and references
instead of treating every source file like ordinary text.

Current behavior preserves the generic baseline: code results are still
`source_assets` plus `asset_chunks`, embedded like other corpus chunks, and
retrievable through `kb.search`, REST search, CLI search, and MCP wrappers.
The foundation now adds parser-backed chunks, durable symbols/references,
code-aware filters, exact-symbol ranking, generated-file suppression, notebook
cells, a public-safe `code-heavy` benchmark baseline, benchmark-derived code
gap priorities, and sanitized parser fallback metadata for opted-in
repositories.

Implemented foundation scope:

- Plain-English purpose: cover the kinds of source, config, test, and build
  files real projects contain.
  Code and developer-artifact coverage now includes common source languages,
  frontend markup and stylesheet artifacts, notebooks, build scripts, package
  manifests, infrastructure/config files, API schemas, SQL files, migrations,
  tests, generated-code markers, and patch/diff artifacts.
- Plain-English purpose: split code into meaningful pieces such as functions
  and classes so search lands on the right area.
  Parser-backed chunking now prefers stable semantic boundaries over fixed text
  windows for modules, classes, functions, methods, Python tests/fixtures plus
  decorators/inheritance, JS/TS route handlers/callers, C# namespaces,
  controllers, routes, tests and calls, frontend markup components/events/form
  actions/selectors, stylesheet selectors/custom properties/keyframes/media
  queries, config blocks, SQL objects and migrations, notebook cells, and
  conservative Go/Rust/PowerShell symbols where reliably detectable.
- Plain-English purpose: remember where important code names live and what
  kind of thing they are.
  Durable symbol metadata stores symbol name, kind, language, file path, line
  and byte ranges, parent symbol, exported/public flag where detectable,
  signature when safe, parser status, confidence, scope hash, and chunk link.
- Plain-English purpose: use real code parsers when available, but keep indexing
  useful when a parser cannot handle a file.
  The parser abstraction uses Python `ast` first, conservative SQL/JS/TS/C#/HTML
  markup/stylesheet/config adapters where reliable, and explicit sanitized
  fallback metadata for unsupported languages or parser failures.
- Plain-English purpose: store code facts in a way that can be searched,
  explained, and linked back to files.
  Durable `code_symbols` and `code_references` tables are tied back to
  `source_assets` and `asset_chunks` so code results still participate in normal
  corpus retrieval and provenance flows.
- Plain-English purpose: show how code pieces relate so Codex can answer
  questions like "who calls this?" or "where are the tests?"
  The foundation captures definitions, imports, calls, inheritance, decorators,
  routes, tests, fixtures, UI component/event/form/selector links, CSS symbols,
  SQL migrations, notebooks, and config facts where the parser can produce
  reliable evidence. The roadmap does not imply perfect static analysis across
  all languages or dynamic frameworks.
- Plain-English purpose: keep answers focused on the selected project instead
  of mixing unrelated code from elsewhere.
  Search now scopes code results by monitored root, repository/root name,
  language, symbol kind, relationship, and path glob so Codex can ask targeted
  questions such as "Find the implementation of X", "Show route handlers for Y",
  and "Find callers/references of this symbol".

Codex-facing retrieval surfaces:

- Plain-English purpose: avoid breaking existing search users while adding code
  intelligence.
  Keep existing `kb.search`, REST search, CLI search, and corpus asset/chunk
  lookup backward compatible.
- Plain-English purpose: let callers narrow searches by language, path, symbol
  type, or code relationship.
  Generic search filters now include `file_kind`, `language`, `symbol_kind`,
  `path_glob`, `relationship`, `include_generated`, and `repo`/`root_name` on
  the existing search contract. Generated code is excluded when
  `include_generated=false` is present in the active filter set.
- Plain-English purpose: add clearer code-search commands where generic search
  would be too crowded.
  Dedicated MCP/CLI/REST code status, search, symbol, and feedback surfaces now
  complement the generic search filters without replacing normal corpus search.
- Plain-English purpose: make code answers cite where they came from and why
  they matched.
  Search results return enough structured metadata for Codex to cite the
  defining file, line range, symbol kind, language, generated flag,
  relationship type, target/source symbols, route/test target where available,
  parser/fallback status, and associated chunk without exposing raw private code
  outside the normal private corpus retrieval path.

Ranking requirements:

- Plain-English purpose: if the user names an exact function or file, that
  should win over a vague similarity match.
  Exact symbol and path matches should beat semantic guesses.
- Plain-English purpose: prefer the project the user is actually working in.
  Local workspace, selected root, and repository evidence should outrank
  unrelated corpus matches.
- Plain-English purpose: tell users whether a result defines something, uses
  it, tests it, or configures it.
  Definitions should be distinguishable from references, callers, imports,
  tests, examples, and configuration.
- Plain-English purpose: keep supporting files findable without confusing them
  with main implementation code.
  Tests, examples, migrations, generated files, and config should remain
  discoverable, but should not be mixed indistinguishably with implementation
  results unless requested.
- Plain-English purpose: show enough evidence to debug surprising code-search
  results.
  Parser-confidence, fallback status, symbol kind, path proximity, import/call
  relationships, file recency, duplicate/version suppression, and existing
  retrieval explanations should be visible enough to debug surprising results.

Privacy and safety constraints:

- Plain-English purpose: Flux should only inspect code locations the user chose.
  Indexing remains opt-in through monitored roots and workspace scopes.
- Plain-English purpose: keep this public repo safe even when private code is
  indexed locally.
  Public repository docs, fixtures, and tests must not contain raw private code,
  private paths, generated private wiki exports, credentials, embeddings, or
  local runtime database values.
- Plain-English purpose: diagnostics should explain what happened without
  leaking code content.
  Stored operational telemetry for parser failures, worker history, benchmark
  runs, and dashboard diagnostics must avoid raw code content unless that
  content is already part of private corpus storage.
- Plain-English purpose: test code-search behavior with fake projects, not user
  repositories.
  Public tests must use synthetic fixture repositories only, with small invented
  symbols, routes, configs, tests, and references.

Out of scope for this slice:

- Full IDE replacement.
- Perfect cross-language static analysis or full IDE replacement behavior.
- Runtime tracing or profiling.
- Provider-specific accelerated embedding backends.
- VSS snapshot extraction.
- Automatic ranking mutation, librarian-worker actions, or automated governance
  decisions from code feedback.

Foundation implementation breakdown:

1. Priority: P0. Status: implemented. Model-actionable item: expand code and developer-artifact
   classification for opted-in repositories, including source files, tests,
   configs, manifests, API schemas, SQL, infrastructure files, notebooks, and
   generated-code markers.
   Plain-English explanation: Flux first needs to recognize the files Codex is
   likely to ask about before deeper symbol search can add value.
2. Priority: P0. Status: implemented. Model-actionable item: add public-safe synthetic fixture
   repositories that cover small invented implementations, tests, configs,
   routes, imports, generated files, unsupported languages, and parser failures.
   Plain-English explanation: The feature needs correctness tests before it can
   safely index private repositories or change retrieval ranking.
3. Priority: P0. Status: implemented. Model-actionable item: implement a parser abstraction that
   produces stable semantic chunks for modules, classes, functions, methods,
   routes, SQL objects, notebook cells, tests, Python class relationships, C#
   route/test/call relationships, frontend markup relationships, and stylesheet
   symbols when reliable, with explicit sanitized fallback metadata when parsing
   is unavailable or fails.
   Plain-English explanation: Codex gets quick value when search lands on the
   right function or test, while fallback behavior keeps indexing useful for
   unsupported files.
4. Priority: P0. Status: implemented. Model-actionable item: add durable code symbol and reference
   storage tied to `source_assets` and `asset_chunks`, including symbol name,
   kind, language, file path, range metadata, parent symbol, definition/reference
   relationship, parser status, and safe provenance.
   Plain-English explanation: A stored symbol layer is the core data foundation
   that turns generic corpus search into code-aware retrieval.
5. Priority: P0. Status: implemented. Model-actionable item: add repository-scoped code retrieval and
   ranking so exact symbol/path matches, local workspace evidence, definitions,
   references, tests, configs, and examples are distinguishable in results.
   Plain-English explanation: This completes the basic user-facing behavior:
   Codex can ask where code lives and receive focused, explainable answers.
6. Priority: P1. Status: implemented. Model-actionable item: expose code-aware
   retrieval through the least-surprising MCP, CLI, and REST contract.
   Plain-English explanation: Tooling should be easy for Codex and scripts to
   call without breaking existing `kb.search` users.
7. Priority: P1. Status: implemented. Model-actionable item: add dashboard and diagnostic views for
   code index coverage, parser failures, fallback rates, slow files, language
   coverage, generated-file counts, top code gaps, direct code search/symbol
   lookup, and privacy-safe per-repository status.
   Plain-English explanation: Operators need to see what indexed well and what
   fell back before trusting code-aware results on real projects. Deeper parser
   drill-down and ranking-tuning work now depends on repeated live retrieval
   feedback rather than more evidence infrastructure.

Acceptance criteria:

- Codex can query the index for a known symbol in a synthetic repository and get
  the defining file/chunk before generic text matches.
- Codex can distinguish definitions, references, tests, configuration, examples,
  imports, and callers in results.
- Existing `kb.search`, REST search, CLI search, and corpus asset/chunk lookup
  remain backward compatible.
- REST, CLI, MCP, and dashboard code-search surfaces accept `relationship`,
  `path_glob`, and `include_generated` without requiring callers to change
  existing requests.
- Unsupported languages still index as text with clear fallback metadata.
- Parser failures are visible as sanitized diagnostics in worker/status/debug
  surfaces.
- No private paths, raw private code, credentials, embeddings, or private corpus
  content appear in public fixtures, docs, tests, telemetry summaries, or
  benchmark records.

## V4: Collaboration And Transfer

| Piece | Priority | Plain-English Purpose | Roadmap Intent | Status | Progress % | Current Evidence / Remaining Gap | Remaining Work | Queued Next |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Team/shared vault mode | P3 | Let multiple people share knowledge only after personal safety rules are proven. | Team/shared vault mode. | planned | 0% | Personal/local-first mode remains the focus. | Define trust, visibility, and audit boundaries after single-user governance matures. | Define trust, visibility, and audit boundaries after single-user governance matures. |
| Sync and export policies | P3 | Decide what can move between machines or users and under what rules. | Sync and export policies. | planned | 0% | Export exists for local audit; multi-user sync/export governance is not started. | Design policy model after shared vault requirements are clear. | Design policy model after shared vault requirements are clear. |
| Optional graph backend | P3 | Consider a specialized relationship database only if PostgreSQL stops being enough. | Optional Apache AGE graph backend. | planned | 0% | PostgreSQL remains the primary store; optional AGE is not started. | Evaluate after graph traversal and lifecycle semantics stabilize. | Evaluate after graph traversal and lifecycle semantics stabilize. |
| Synthetic data and fine-tuning | P3 | Use generated safe data later to improve testing or training without exposing private content. | Synthetic-data and fine-tuning pipeline. | planned | 0% | Not started. | Defer until evaluation and governance foundations are in place. | Defer until evaluation and governance foundations are in place. |

## Queued Work In Roadmap Order

The queue uses the same priority labels as the detailed roadmap item tables.

Queued items are intentionally larger related slices so one model run can spend
more tokens on implementation and verification instead of repeated planning,
branch setup, and context rebuilding.

Queue policy: chip away at roadmap items in priority order from P0 to Pn. Do not force blocked P0 items into the active queue ahead of their prerequisites;
keep each blocked P0 visible with its blocker and wake condition.

Completed P0/P1 bundles: retrieval evaluation foundation, retrieval calibration
follow-through, the operator evidence/diagnostics accelerator, the first
operator remediation/corpus completion slice, expanded code-aware retrieval
follow-through, and the memory review closure/governance evaluation gate. Flux now has
metadata-only synthetic retrieval benchmarks, indexer reliability gates,
multi-root reliability readiness, full all-root evidence runs, operator evidence
gate decisions, privacy-safe code diagnostics/search/symbol surfaces, code
retrieval feedback summaries, benchmark-derived code gap priorities,
relationship/path/generated filters, read-only filtered operational diagnostics,
dashboard evidence panels, confirmation-gated diagnostic remediation actions,
comparison deltas, confidence-band summaries, semantic duplicate calibration candidates,
richer suppression/deprioritization explanations, sample-first
large structured-file indexing including converted legacy/OpenDocument
spreadsheets, approved Codex backfill ingestion, sanitized capture-ingestion
status, governance-shadow benchmark evidence, and `settings_mutated: false`. It
remains conservative: live
private query sets, automatic ranking/threshold/settings mutation, VSS
extraction, provider-specific acceleration, full IDE/static-analysis
replacement, and automatic librarian-worker mutation are still out of scope.

Queue triage found several P0/P1 rows that were not represented directly in the
active queue. Approved-capture ingestion, governance/librarian shadow evaluation,
and review UX closure across capture/retention/retrieval evidence are now
implemented as the completed gate below. Remaining implementation work shifts to
evaluated P2 governance automation and lower-priority corpus coverage; the
live-evidence-only P0/P1 work remains visible below with its blocker and wake
condition instead of blocking executable roadmap progress.

1. Priority: P0/P1. Plain-English purpose: close the memory review loop before adding automatic
   memory governance. Bundle: Memory Review Closure And Governance Evaluation
   Gate. Status: completed in `codex/memory-review-governance-gate`. Progress:
   review workflows 88-90%, retrieval benchmarks 82%, historical backfill 35%,
   memory governance prerequisites 20%.
   Model-actionable item completed: approved `codex_backfill` processing now
   runs through redaction and relevance checks, durable `remember` writes,
   sanitized provenance, audit records, REST/CLI/MCP ingestion surfaces, review
   dashboard status, retention-quality context, and the metadata-only
   `governance-shadow` retrieval benchmark suite.
   Remaining Work: keep automatic lifecycle mutation, reversible apply/recover,
   operator digests, scheduled librarian workers, and local-model rationale for
   the later P2 automation bundle.
   Plain-English explanation: approved review and evaluation are the missing
   safety gate between today's manual review tools and tomorrow's automation.
2. Priority: P0. Plain-English purpose: prove indexer and filesystem reliability
   on real workloads before adding snapshot extraction. Progress: 93%.
   Model-actionable item: keep using `flux-kb acceleration evidence` and
   `flux-kb acceleration reliability run --scope all-roots --full` with labels
   and comparisons to validate high-volume roots, cloud-sync delayed
   availability, open Office files, large writes, rename-save patterns, watcher
   reconciliation, decoupled heartbeat freshness, queued watch-event sync jobs,
   lock/churn evidence, worker-cap telemetry, and default-cap tuning from
   observed hardware evidence.
   Blocker: this is now primarily live validation after the heartbeat/worker
   hardening implementation slice.
   Wake condition: comparable full all-root runs show repeated reliability gaps
   or provide enough evidence to justify VSS/provider-specific design work.
   Remaining Work: run and compare live all-root evidence after deployments,
   decide whether any gaps justify a later VSS design, and keep
   provider-specific acceleration blocked until telemetry justifies design work.
3. Priority: P0. Progress: 91%. Plain-English purpose: make code-aware
   retrieval diagnosable after feedback exists.
   Model-actionable item: validate the expanded code-aware retrieval path on
   live opted-in repositories using code coverage views, parser
   failure/fallback summaries, privacy-safe per-repository status,
   relationship coverage, direct code-search/symbol surfaces, hashed code
   retrieval feedback, and retrieval benchmark code gaps. Explain-time
   filters now feed raw retrieval before truncation so route, test,
   generated-file, path, relationship, and logical-kind checks measure the
   intended candidate set. Broad search, brief, and explain now exclude code
   unless the query carries `file_kind=code` / `filters={"file_kinds":["code"]}`
   as the only file-kind request; mixed code plus non-code file-kind filters are rejected.
   Blocker: expanded C#, Python, frontend markup, stylesheet parser coverage, balanced generic/code-focused ranking, and explain-time code filters are implemented; further parser/ranking expansion is blocked until retrieval benchmark/live feedback identifies repeated miss evidence.
   Wake condition: privacy-safe feedback or retrieval benchmark gaps recur
   around the same symbol, relationship, parser, path, or ranking failure.
   Remaining Work: tune explicit-code ranking and add only targeted deeper parsers from
   repeated benchmark/live misses; keep raw private code/queries, automatic
   ranking mutation, and full IDE/static-analysis replacement out of scope.
4. Priority: P1. Progress: 89%. Plain-English purpose: make daily operation
   inspectable and recoverable without raw logs.
   Model-actionable item: validate operator diagnostics remediation against
   recurring live failures: retrieval drill-downs, watcher event summaries,
   worker heartbeat/history, mail sync/post-process evidence, filtered blocked
   dependency views, search focus controls, code-heavy result diagnostics,
   slow-job history, retry/scoped-backfill/root-cleanup actions, and sample-first indexing for large tabular and converted
   legacy/OpenDocument spreadsheet files.
   Blocker: the recovery surfaces exist; more mutation/recovery links need
   repeated operator evidence rather than speculative expansion.
   Wake condition: recurring live failures show which deeper recovery link,
   diagnostic page, or remediation action would reduce repeated operator work.
   Remaining Work: run live validation, add only higher-value recovery links
   discovered from repeated operator use, and keep optional full structured
   backfill behind trusted sample-first metadata.
5. Priority: P1. Plain-English purpose: finish practical corpus coverage where local extractors already have safe
   metadata-first fallback paths. Bundle: Corpus Coverage Completion Bundle.
   Status: completed in `codex/corpus-coverage-completion`. Progress: corpus
   expansion 86-88%.
   Completed slice: added practical file-kind taxonomy coverage, bounded local
   parsers for subtitles, mail exports, calendar/contact files, PSV/SSV,
   NDJSON/JSON-LD, SARIF/SPDX/CycloneDX, JUnit/TRX/TAP, LCOV/coverage XML, HAR,
   SQLite schema metadata, and sensitive metadata-only formats; extended
   optional-tool availability diagnostics,
   archive embedded-member parsing, worker telemetry counts, CLI/REST kind
   aliases, `file-type-coverage.md`, and architecture/roadmap docs.
   Remaining Work: prioritize specialized local-tool stages and optional full
   structured backfill only where sample-first diagnostics show value; avoid
   mixing this with governance automation, VSS/provider acceleration, or code
   parser tuning.
6. Priority: P2. Plain-English purpose: improve memory quality with reversible evaluated
   automation. Bundle: Evaluated Memory Governance Automation. Progress: 70%.
   Model-actionable item: build from the implemented governance run/action/digest
   layer by calibrating live proposal quality, tuning protected-memory rules,
   and expanding only target-specific recovery-proven semantic presentation
   actions. The current slice adds explicit librarian-worker shadow mode,
   benchmarked apply gates, reversible claim lifecycle actions, stale tagging,
   retrieval deprioritization, semantic/canonical cluster proposals, recovery
   views, audit records, operator digests, and optional local-only
   model rationale settings with rule-based fallback.
   Remaining Work: run live private governance-shadow and proposal review
   cycles, tune precision thresholds and cadence, validate recovery UX on real
   applied actions, and defer broader local-model consolidation until proposal
   precision stays stable.
   Plain-English explanation: routine cleanup is now proposed and recoverable,
   but wider automation still waits for live benchmark evidence and manual
   review feedback.
7. Priority: P3. Progress: 0%. Plain-English purpose: keep optional scale-outs
   behind proven local value.
   Model-actionable item: defer optional ParadeDB/BM25, shared-vault
   collaboration, sync/export governance, Apache AGE, and
   synthetic-data/fine-tuning until single-user retrieval, code indexing,
   indexer reliability, diagnostics, evaluation, and recovery flows are stable.
   Remaining Work: revisit only after the local single-user system is correct,
   inspectable, recoverable, and useful under real workloads.
   Plain-English explanation: these may matter later, but they should not spend
   roadmap or model context before the local single-user knowledge system is
   correct, inspectable, and useful.

## Update Rules

- Update this file in the same commit as any roadmap-significant feature.
- After every roadmap-significant session or turn, update affected `Progress %`
  and `Remaining Work` entries before closeout.
- Keep roadmap status factual and conservative.
- Prefer `in progress` over `shipped` unless the piece has working code,
  documentation or UI where applicable, and verification.
- Link to docs, tests, or commits when a status changes materially.
- Never record private runtime state, private file paths, mail contents, tokens,
  raw memories, embeddings from private content, or database dumps.
