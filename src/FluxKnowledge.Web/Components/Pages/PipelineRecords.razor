@page "/pipeline-records"
@inject NavigationManager Navigation

<PageTitle>Corpus | Flux Knowledge</PageTitle>
<p role="status">Opening Corpus…</p>

@code {
    protected override void OnInitialized() => Navigation.NavigateTo("/corpus", replace: true);
}
